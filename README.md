# Final Project: Emotion Detector

AI-based Flask web application that detects the dominant emotion in text.

The application exposes an `EmotionDetection` Python package, unit tests, and a
Flask server for web deployment.
